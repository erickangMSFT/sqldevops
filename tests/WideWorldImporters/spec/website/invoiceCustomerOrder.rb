describe 'WideWorldImporters: Website.InvoiceCustomerOrders' do
    it 'successfully creates an invoice for customer orders' do
        raise ("todo")
    end

    it 'fails for an invalid order' do
        raise ("todo")
    end

    it 'fails for an invalid packed person id' do
        raise("todo")
    end

    it 'fails for an invalid invoiced by person id' do
        raise("todo")
    end

    it 'fails if the order is not picked' do
        raise ("todo")
    end

    it 'fails if the order is already invoiced' do
        raise ("todo")
    end

    it 'fails to create a new invoice number' do
        raise ("todo")
    end
end